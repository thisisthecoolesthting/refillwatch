---
// AdSlot — placeholder for future display ad integration.
// Renders nothing until an ad provider (AdThrive, Mediavine, etc.) is wired up.
interface Props { position?: string; label?: string; }
const { position, label } = Astro.props;
---
<!-- ad-slot:{label ?? position} (not yet wired) -->
